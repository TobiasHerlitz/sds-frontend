import styled from 'styled-components';

const root = styled.div`
  height: 100vh;
  width: 100vw;
  max-width: 1200px;
  margin: auto;
  // display: flex;
  // flex-direction: row;
  // justify-content: center;
`;

export default {
  root
}